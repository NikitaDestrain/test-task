<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!--property name="hibernate.hbm2ddl.auto">update</property-->
        <!--property name="hibernate.hbm2ddl.import_files">database/scripts/schema-generation.sql</property-->
        <!-- Database Connection settings -->
        <property name="connection.driver_class">org.hsqldb.jdbc.JDBCDriver</property>
        <property name="connection.url">jdbc:hsqldb:file:database/hsqldb/db;</property>
        <property name="connection.username">SA</property>
        <property name="connection.password"/>

        <!-- Enable the logging of all the generated SQL statements to the console -->
        <property name="show_sql">true</property>

        <!-- Format the generated SQL statement to make it more readable, -->
        <property name="format_sql">true</property>

        <!-- Hibernate will put comments inside all generated SQL statements to
         hint what’s the generated SQL trying to do -->
        <property name="use_sql_comments">true</property>

        <!-- This property makes Hibernate generate the appropriate SQL for the
         chosen database. -->
        <property name="dialect">org.hibernate.dialect.HSQLDialect</property>

        <!-- mappings for annotated classes -->
        <mapping class="com.haulmont.testtask.domain.Specialization"/>
        <mapping class="com.haulmont.testtask.domain.Doctor"/>
        <mapping class="com.haulmont.testtask.domain.Patient"/>
        <mapping class="com.haulmont.testtask.domain.Priority"/>
        <mapping class="com.haulmont.testtask.domain.Recipe"/>
    </session-factory>
</hibernate-configuration>